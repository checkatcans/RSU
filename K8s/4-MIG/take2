kubectl patch clusterpolicies.nvidia.com/cluster-policy \
    --type='json' \
    -p='[{"op":"replace", "path":"/spec/mig/strategy", "value":"single"}]'

kubectl label nodes rsu-inference nvidia.com/mig.config=all-3g.47gb --overwrite
kubectl label nodes rsu-inference nvidia.com/mig.config=all-3g.47gb --overwrite

kubectl get node rsu-inference -o=jsonpath='{.metadata.labels}' | jq .


Disabling MIG
You can disable MIG on a node by setting the nvidia.con/mig.config label to all-disabled:

kubectl label nodes rsu-inference nvidia.com/mig.config=all-disabled --overwrite
